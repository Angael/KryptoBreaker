import { createTheme } from '@material-ui/core';

const theme = createTheme({
    palette: {
        primary: {
            main: '#388e3c',
        },
        secondary: {
            main: '#ffb74d',
        },
    },
});

export default theme;
